import React from "react";

import { Comic } from "@/types/data";

type CardComicInfoProps = Comic;

const CardComicInfo = ({ ...props }: CardComicInfoProps) => {
  return <div>CardComicInfo</div>;
};

export default CardComicInfo;
