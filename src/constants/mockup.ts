import { Category, Comic, Tag, User } from "@/types/data";

const MOCKUP: {
  users: Array<User>;
  tags: Array<Tag>;
  categories: Array<Category>;
  comics: Array<Comic>;
} = {
  users: [],
  tags: [],
  categories: [],
  comics: [],
};

export default MOCKUP;
